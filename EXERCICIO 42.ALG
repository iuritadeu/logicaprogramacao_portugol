algoritmo "exercicio"
var
cont, N : inteiro

inicio
cont <- 0
Escreva("Quer contar até que número? ")
Leia (N)
     Enquanto (cont < N) faca
      escreval (cont)
      cont <- cont + 1
     FimEnquanto
     Se (cont = N) entao
        Escreval (cont, " Acabou!")
     FimSe


fimalgoritmo